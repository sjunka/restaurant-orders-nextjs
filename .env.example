# SQLite database file (relative to project root, created automatically)
DATABASE_URL="file:./dev.db"

# Shown in the UI and attached to every cart/order event
NEXT_PUBLIC_MOCK_USER_ID=user-mock-001

# Server-side pricing constants
TAX_RATE=0.08
SERVICE_FEE_CENTS=150
